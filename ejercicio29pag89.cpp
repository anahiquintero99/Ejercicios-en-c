#include<stdio.h>
#include<conio.h>

 main(){
 	float x, y;
 	printf("Resolver la ecuacion\n x/y=1\n 5x+y=12 ");
 	x=(-1*12)+(1*1)/(-1*1)+(1*1);
 	y=(-1*5)+(12*1)/(-1*5)+(1*1);
 	printf("El resultado de x es: %f", x);
 	printf("\nEl resultado de y es: %f", y);
 }
